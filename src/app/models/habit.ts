export class Habit { // у нас это interface называется. часто вижу, что где-то пишут как class это, но тут от class только переменные и типы. 
    name!: string;
    frequency!: string;
    description!: string;
}
